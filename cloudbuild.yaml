steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/your-app-name:$COMMIT_SHA', '.']

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/your-app-name:$COMMIT_SHA']

# If you are deploying to Cloud Run, you might add a deploy step here:
# - name: 'gcr.io/cloud-builders/gcloud'
#   args:
#     - 'run'
#     - 'deploy'
#     - 'your-app-name' # Replace with your desired Cloud Run service name
#     - '--image'
#     - 'gcr.io/$PROJECT_ID/your-app-name:$COMMIT_SHA'
#     - '--region'
#     - 'us-central1' # Replace with your desired region
#     - '--platform'
#     - 'managed'
#     - '--allow-unauthenticated' # Add this if your service should be publicly accessible